name: J.A.R.V.I.S. Android Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
          channel: 'stable'

      - name: System Diagnostics
        run: |
          flutter doctor -v
          ls -la
          
      - name: Surgical Android Platform Restoration
        run: |
          # Back up modernized scripts
          cp android/settings.gradle settings.gradle.bak
          cp android/app/build.gradle build.gradle.bak
          
          # Restore missing files
          flutter create --platforms=android .
          
          # Restore optimized scripts
          mv settings.gradle.bak android/settings.gradle
          mv build.gradle.bak android/app/build.gradle
          
          chmod +x android/gradlew

      - name: Prepare Android Build
        run: |
          echo "flutter.sdk=$FLUTTER_ROOT" > android/local.properties
          
          if [ ! -f android/app/debug.keystore ]; then
            keytool -genkey -v -keystore android/app/debug.keystore \
              -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 \
              -validity 10000 -dname "CN=Android Debug,O=Android,C=US" -noprompt
          fi
          
          flutter clean
          flutter pub get

      - name: Build APK (Clean Slate)
        id: build_apk
        run: |
          # 1. PURGE AND REGENERATE
          rm -rf android/
          flutter create --platforms=android --org com.jarvis --project-name iamandroidjvs .
          
          # 2. SURGICAL PATCH - Manifest Permissions & Label
          sed -i '/<uses-permission/d' android/app/src/main/AndroidManifest.xml
          sed -i '/<manifest/a \    <uses-permission android:name="android.permission.RECORD_AUDIO" />\n    <uses-permission android:name="android.permission.INTERNET" />\n    <uses-permission android:name="android.permission.CAMERA" />' android/app/src/main/AndroidManifest.xml
          sed -i 's/android:label="iamandroidjvs"/android:label="J.A.R.V.I.S."/' android/app/src/main/AndroidManifest.xml
          
          # 3. BUILD
          flutter build apk --release --verbose --obfuscate --split-debug-info=build/app/outputs/symbols
          
          # 4. CAPTURE
          cp build/app/outputs/flutter-apk/app-release.apk ./app-release.apk

      - name: Upload APK on Success
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: jarvis-android-release
          path: app-release.apk
